---
date: '2013-06-22'
title: '自由绘制'
description: '使用FabricJS自由绘制'
thumbnail: 'free-drawing.png'
tags: ['绘制', '刷子']
---

import './index.css';
import code from './code?raw';
import { CodeEditor } from '../../../components/CodeEditor';
import { FreeDrawingUI } from './FreeDrawingUI';

<CodeEditor code={code} canvasId="c" client:idle>
<div className="demo-body">
	<canvas id="c" width="500" height="500"></canvas>

    <div id="drawing-mode-options">
    	<button id="drawing-mode" className="btn btn-info bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">取消绘制模式</button>
    	<button id="clear-canvas" className="btn btn-info bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">清除</button>
    	<label htmlFor="drawing-mode-selector">
    		绘制模式:
    		<select id="drawing-mode-selector">
    			<option>铅笔(Pencil)</option>
    			<option>圆形(Circle)</option>
    			<option>喷溅(Spray)</option>
    			<option>图案(Pattern)</option>
    			<option>横线(hline)</option>
    			<option>竖线(vline)</option>
    			<option>正方形(square)</option>
    			<option>菱形(diamond)</option>
    			<option>纹理(texture)</option>
    		</select>
    	</label>
    	<label htmlFor="drawing-line-width">
    		线宽:
    		<span className="info">30</span>
    		<input type="range" min="0" max="150" id="drawing-line-width" />
    	</label>
    	<label htmlFor="drawing-color">
    		线颜色:
    		<input type="color" id="drawing-color" />
    	</label>
    	<label htmlFor="drawing-shadow-color">
    		阴影颜色:
    		<input type="color" id="drawing-shadow-color" />
    	</label>
    	<label htmlFor="drawing-shadow-width">
    		阴影宽度:
    		<span className="info">0</span>
    		<input type="range" min="0" max="50" id="drawing-shadow-width" />
    	</label>
    	<label htmlFor="drawing-shadow-offset">
    		阴影偏移:
    		<span className="info">0</span>
    		<input type="range" min="0" max="50" id="drawing-shadow-offset" />
    	</label>
    </div>

</div>
</CodeEditor>
<FreeDrawingUI />
